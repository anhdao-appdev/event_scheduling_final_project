<!DOCTYPE html>
<html>
  <head>
    <title>Booth Debauchery - Where the fun begins!</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
  </head>

  <body>
  
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" data-ol-has-click-handler=""><b>Booth Debauchery</b></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          <% if @current_user!=nil %>
            <li class="nav-item">
              <a class="nav-link" href="/events" data-ol-has-click-handler="">Events Calendar</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/new_event" data-ol-has-click-handler="">Create New Event</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/user_events" data-ol-has-click-handler="">Your Events</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/edit_user_profile" data-ol-has-click-handler="">Your Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/user_sign_out" data-ol-has-click-handler="">Sign Out</a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
    </nav>
    <br>

    <% if flash[:notice]!=nil %>
      <div class="alert alert-dismissible alert-success">
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert]!=nil %>
      <div class="alert alert-dismissible alert-danger">
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        <%= flash[:alert] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>